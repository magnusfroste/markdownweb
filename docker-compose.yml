services:
  markdownweb:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      MCP_ADMIN_KEY: "${MCP_ADMIN_KEY:?set MCP_ADMIN_KEY}"
      PORT: 3000
      HOST: 0.0.0.0
      NODE_ENV: production
